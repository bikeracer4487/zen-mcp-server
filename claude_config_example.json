{
  "comment": "Example Claude Desktop configuration for Doug-Zen MCP Server",
  "comment2": "Run './run-server.sh -c' to get the exact configuration for your system",
  "comment3": "For platform-specific examples, see the examples/ directory",
  "mcpServers": {
    "doug-zen": {
      "command": "/path/to/doug-zen-mcp-server/.doug-zen_venv/bin/python",
      "args": ["/path/to/doug-zen-mcp-server/server.py"]
    }
  }
}